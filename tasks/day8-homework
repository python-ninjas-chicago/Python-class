# 1)Create a Temprature class and two functions in class :
# Function 1 - takes celsius and convert to Fahrenheit.
# Function 2 - takes Fahrenheit and convert to Celsius.
#
# Use formula below to convert:
# celsius*9/5+32
# farenhiet-32*5/9

class Temprature:
    def __init__(self, cels, fahr):
        self.unite_name = cels
        self.unite1_name = fahr


    def convert_c_to_f(self, count):
        return f"Today's temprature is {count * 9 / 5 + 32} {self.unite1_name}"

    def convert_f_to_c(self, count1):
        return f"Today's tempature is {(count1 - 32) * 5 / 9} {self.unite_name}"

t1 = Temprature('Celsius', 'Fahrenheit')
print(t1.convert_c_to_f(15))
print(t1.convert_f_to_c(100))
